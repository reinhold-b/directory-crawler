# Python Directory Crawler

Crawls a directory recursively and counts lines of text/code in all files in the base or subdirectory.
Uses recursion.

Open the crawler, select a directory and wait for the result.
